# Parity fixtures

Shared fixtures pinning the Python and JavaScript evaluators to each
other: each fixture is a spec plus parameter values plus the expected
vertex and face arrays. Both evaluators' test suites run every fixture;
counts and ordering must match exactly, coordinates within the tolerance
each fixture declares.

Empty until spec v1 lands its first vertical slice.
